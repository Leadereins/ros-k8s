# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with Docker assets in this directory.

## Docker

```bash
docker build -f docker/Dockerfile -t ros-k8s:latest .
```
